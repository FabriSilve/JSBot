version: '2'
services:
  mongodb:
    image: "mongo"
    ports:
     - "6300:6300"
  api:
    build: ./api/
    ports:
      - "6200:6200"
    volumes:
      - ./api:/usr/src/app
    depends_on:
      - mongodb
